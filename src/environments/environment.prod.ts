const HeroBaseUrl = 'https://cdn.rawgit.com/akabab/superhero-api/0.2.0/api';

export const environment = {
  production: true,
  herosApis: {
    all: HeroBaseUrl + '/all.json',
    id: HeroBaseUrl + '/id'
  }
};
